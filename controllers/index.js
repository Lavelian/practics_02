const {
  getAll,
  createContact,
  removeContact,
} = require("./contacts");

const {
  getAllNote,
  createNote,
  removeNote,
} = require("./note");

module.exports = {
  getAll,
  createContact,
  removeContact,
  getAllNote,
  createNote,
  removeNote,
};
